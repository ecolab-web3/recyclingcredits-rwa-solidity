{
  "name": "recyclingcredits-rwa-solidity",
  "version": "1.0.0",
  "description": "A smart contract prototype that tokenizes Recycling Credits as Real-World Assets (RWA).",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "coverage": "hardhat coverage",
    "deploy:fuji": "hardhat run scripts/deploy.ts --network fuji"
  },
  "author": "E-co.lab Dev Team",
  "license": "MIT",
  "dependencies": {
    "@openzeppelin/contracts": "^5.4.0"
  },  
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "dotenv": "^17.2.1"
  }
}
